package com.myprojects.ticket.service.enums;

/**
 * Enum of different types of events occur in ticket-service
 */
public enum TicketServiceEvent {
	HOLD_RESERVATION,
	HOLD_RESERVATION_EXPIRED,
	CONFIRM_RESERVATION
}
